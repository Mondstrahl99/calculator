services:
  web-calc:
    image: nginx:latest
    ports:
      - 80:80
    networks:
      - net
    volumes:
      - './html:/usr/share/nginx/html'
  
  browser:
    image: fathyb/carbonyl:latest
    networks:
      - net

networks:
  net:

